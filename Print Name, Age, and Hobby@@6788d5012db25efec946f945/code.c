#include <stdio.h>
int main() {
    int age;
    char name[100],b[100];
    scanf("%s" "%d" "%s", &name, &age, &b);
    printf("Name: %s\n", name);
    printf("Age: %d\n", age);
    printf("Hobby: %s", b);
    return 0;
}